import { TargetActionParam } from './target-action-param';

describe('TargetActionParam', () => {
  it('should create an instance', () => {
    expect(new TargetActionParam()).toBeTruthy();
  });
});
