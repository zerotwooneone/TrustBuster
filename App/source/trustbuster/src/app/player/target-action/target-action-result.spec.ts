import { TargetActionResult } from './target-action-result';

describe('TargetActionResult', () => {
  it('should create an instance', () => {
    expect(new TargetActionResult()).toBeTruthy();
  });
});
